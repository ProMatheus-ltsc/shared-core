# 本地开发环境变量（此文件已被 gitignore，不会提交）
# 复制为 .dev.vars 并按需填写：
#   SYNC_AUTH_TOKEN=可选，仅当 wrangler secret put SYNC_AUTH_TOKEN 设置了同名值时才需要一致
# D1 database_name / database_id 已直接写在 wrangler.toml，无需在此重复配置。
SYNC_AUTH_TOKEN=
